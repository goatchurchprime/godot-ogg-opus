#pragma once

#include <godot_cpp/core/class_db.hpp>

void initialize_xiph_audio_module(godot::ModuleInitializationLevel p_level);
void uninitialize_xiph_audio_module(godot::ModuleInitializationLevel p_level);
